name: bid-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13.7
  - pip
  - pip:
    # LangChain ecosystem
    - langchain>=1.2.0
    - langgraph>=1.0.5
    - langchain-openai>=1.1.6

    # LLM providers
    - google-genai>=1.0.0

    # Document processing
    - python-docx>=1.1.0
    - Pillow>=10.0.0

    # Configuration & utilities
    - python-dotenv>=1.2.1
    - pydantic>=2.0.0
